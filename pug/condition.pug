doctype html
html
  head 
    //- stylesheet
    link(rel ="stylesheet" href="public/css/style.css")
    //- in file
    style.
      h1 {
        color: red;
        text-align: center;
        padding: 10px 10px;
      }

  body
  
    h1 PUG - Condition
    div.condition 
      - let user = {name : "nguyen", signed: false, lastLogin : 6}
      if (user.signed)
        strong#user-id #{user.name}
        span | logout
      else if user.lastLogin < 10 
        p Your last login was #{user.lastLogin} minutes ago
        p Please login again 
      else 
        a(href="/login") Login now 